class Failure {
  //displays the msg and the code of error
  //understand the error and display it to the user
  final String message;
  final int? code;

  Failure({
    required this.message,
    this.code,
  });
  
  }

