import 'package:flutter/material.dart';

class MyListingsTab extends StatelessWidget {
  const MyListingsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("My Listings will appear here."),
    );
  }
}
